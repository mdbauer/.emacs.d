#include <iostream>
#include <cmath>

#define N 10

using namespace std;

int main() {
  int* x = new int(N);
  for (int i=0; i<N; i++) {
    x[i] = pow(3, i);
    cout << x[i] << endl;
  }
  cout << "Second-to-last element: " << x[N-2] << endl;
  return 0;
}
